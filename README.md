# PlantRX is an application that is used to detect and predict plant diseases early. Read requirements.txt to see which packages
# should be downloaded and installed. This appliation uses a combination of flask to run the application on the local host and
# tensor flow for the machine learning model. Users are able to create an account, login, upload plant images, see classification
# results, and past classificaitons
